t.replace(16,28,40,567) # 替换时间对象【快1】
t2 = t1.replace(hour=15)  # 替换t1的小时数为15【快2】
t3 = t2.replace(fold=1)  # 设置为无效时间，并没有实际意义【快2】
tt = (21, 30, 45, 99999)  # 创建实例化时间对象所需的元组【快3】
t2 = t1.replace(*tt)  # 使用元组替换时间对象的属性，记得加*号【快3】
