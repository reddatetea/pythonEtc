for line in sys.stdin: # 使用sys.stdin读取标准输入流
    if line == '\n': # 如果用户什么都没有输入，直接按下回车
        break # 终止for循环
name, sex, height, weight = (x for x in line.split()) # 使用空格分割用户输入
if username == 'mrsoft' and password == '123456': # 验证用户输入的用户名和密码
